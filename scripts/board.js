MyGame.board = (function () {
    'use strict';

    // holds the state of the game grid
    // 1. playable grid -- includes mappings of row to water, road, safe
    // 2. frog location
    // 3. end points, which endpoints are filled

    function initialize() {
        // setup empty board
        x
    }

    function reset_board() {
        // clears all frogs from board
    }

    function update_frog_position(frogID, new_position) {
        // updates where the frog is
    }

    function remove_frog(frogID) {
        // remove a frog from the board state
    }

    function add_frog(frogID, position) {
        // adds a new frog to the gameboard (frogs at 'home' won't be removed)
    }

    function home_taken(position) {
        // returns if homepad already taken
    }


}());